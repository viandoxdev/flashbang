[package]
name = "fb-web"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
zip = { version = "8.2", features = ["deflate"] }
fb-core = { path = "../fb-core", default-features = false, features = ["wasm"] }
parking_lot = "0.12"
